const path = require('path');

module.exports = {
  // ... Diğer yapılandırma ayarları ...
  output: {
    // ... Diğer output ayarları ...
    publicPath: '/dist/', // Bu satırı ekleyin veya güncelleyin
  },
};